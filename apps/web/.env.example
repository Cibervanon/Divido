# URL base del backend para las peticiones HTTP.
# Local: déjala vacía (Vite usa el proxy /api -> http://localhost:4000).
# Vercel (producción): URL pública de Render, sin barra final.
VITE_API_BASE=https://divido-433u.onrender.com
